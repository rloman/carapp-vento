import { DutchLicensePlatePipe } from './raymie-uppercase.pipe';

describe('RaymieUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new DutchLicensePlatePipe();
    expect(pipe).toBeTruthy();
  });
});
